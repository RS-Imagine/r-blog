# Project Instructions

- Treat `content/posts/*.md` as the source of truth for blog posts.
- Keep the Rust admin app local-only on `127.0.0.1`.
- Generate static pages into `public/` for Cloudflare Pages.
- Prefer small, focused changes that keep the blog simple and free-tier friendly.
